version: 1
scopes: ["services"]
methods:
  GET:
    public: true
    summary: "Get regular schedule for service"
    description: "Returns a regular schedule entry for a specific service"
    responses:
      200: Regular-Schedule
      500: Message
    action: "${dir.src}/actions/services/get_service_regular_schedule.php"