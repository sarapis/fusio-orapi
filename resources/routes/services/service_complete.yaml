version: 1
scopes: ["services"]
methods:
  GET:
    public: true
    summary: "Get service with full details"
    description: "Returns a service entry with full details"
    responses:
      200: Service-Complete
      500: Message
    action: "${dir.src}/actions/services/get_service_complete.php"