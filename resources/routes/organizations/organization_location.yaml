version: 1
scopes: ["organizations"]
methods:
  GET:
    public: true
    summary: "Get location for organization"
    description: "Returns a location entry for a specific organization"
    responses:
      200: Location
      500: Message
    action: "${dir.src}/actions/organizations/get_organization_location.php"